<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AlarmClock" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/cherry_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
<!--        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>-->
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="myTimePickerStyle" parent="@android:style/Widget.Material.Light.TimePicker">
        <item name="android:numbersTextColor">#ffffff</item>
        <item name="android:numbersBackgroundColor">@color/pale_white</item>
        <item name="android:numbersSelectorColor">@color/pale_white</item>
        <item name="android:headerBackground">@color/pale_white</item>
    </style>

    <style name="timePickerWhite">
        <item name="android:textSize">32dp</item>
        <item name="android:textColorPrimary">@color/pale_white</item> <!-- color for digits -->
        <item name="android:textColor">@color/pale_white</item> <!-- color for colon -->
        <item name="android:colorControlNormal">@color/pale_white</item> <!-- color for (horizontal) delimeters -->
    </style>

    <style name="digitalClockTheme">
        <item name="android:textSize">32dp</item>
        <item name="android:fontFamily">@font/kaushan_script</item>
        <item name="android:textColorPrimary">@color/pale_white</item> <!-- color for digits -->
        <item name="android:textColor">@color/pale_white</item> <!-- color for colon -->
    </style>

    <style name="dropDownTheme">
        <item name="android:textColorPrimary">@color/pale_white</item> <!-- color for digits -->
        <item name="android:background">@color/pale_black</item> <!-- color for digits -->
        <item name="android:textColor">@color/pale_white</item> <!-- color for colon -->
    </style>
</resources>